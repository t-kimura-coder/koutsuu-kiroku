# 走行距離メモ 技術資料（他プロジェクト転用向け）

このアプリ（バックエンドサーバーなしのスマホ向けPWA＋PC側Pythonスクリプト）で確立できたパターンと、その適用範囲・限界をまとめたもの。同じ構成が使えそうな別の業務改善アイデアが出たとき、ゼロから設計し直さずにこのメモを起点にする。

## 全体構成

```
[スマホ] PWA（HTML/CSS/vanilla JS、フレームワークなし）
    ↓ 入力・保存
[スマホ内] IndexedDB（構造化データ＋写真）＋ localStorage（設定値）
    ↓ 月1回、OSの共有シート経由
[Box等] メールアップロードで受信 → フォルダに自動振り分け
    ↓
[PC] Pythonスクリプト（win32com等）→ Excel転記
```

サーバーは一切ない。GitHub Pagesで静的ホスティングし、認証なしで誰でも開けるが、各人のデータは各人のスマホの中だけに閉じている。

## できること（このアーキテクチャが得意なパターン）

- **1人1台完結の日々の記録 → 月次まとめて提出**という業務フロー全般
  （走行距離、日報、点検チェックリスト、工事写真台帳 など）
- オフラインで動く入力フォーム（IndexedDBはネットワーク不要）
- カメラ起動なしの写真取り込み（`<input type="file" accept="image/*" capture="environment">`）＋ 端末側での自動リサイズ保存（容量対策）
- 端末の共有シート経由での送信（メール・LINE・Box等、相手を選ばず送れる。サーバー側の受信APIを一切書かなくていい）
- 機種変更・バックアップ対策としての「全データ書き出し/読み込み」機能（JSON一括、写真はBase64同梱）
- PC側の自動集計・Excel転記（Box同期フォルダを定期的にスキャンし、`transcribe.py`的なスクリプトで転記。win32com.clientの`DispatchEx`を使えばユーザーの手元のExcelを巻き込まない）

## できないこと・向かないこと

- **複数人のデータをリアルタイムで一箇所に集約すること**。データは端末ごとに孤立しているので、「今どこの現場に誰がいるか」のような即時共有には向かない。月1回など間隔を置いた「まとめて提出」型でないと成立しない。
- **送信の到達確認**。OSの共有シートを呼び出すところまでしか関知できず、実際にメールが届いたかは分からない（=「送信済み」表示は「共有操作をした」の記録であり、相手に届いた証拠ではない）。
- **端末をまたいだ同期**（同じ人がスマホとPCで同じデータを見る、等）。バックアップ書き出し/読み込みは「移行」はできるが「同期」はできない。
- **バックグラウンドでの確実な処理**。アプリを閉じた瞬間に処理を続けることはできない（`visibilitychange`/`pagehide`で保存の保険は打てるが、それ以上のことは不可）。
- **大量データの高度な検索・横断集計**。IndexedDBは1端末分のデータ量なら問題ないが、複数人・複数年のデータを串刺しで分析するような用途にはそもそも向かない（それが必要ならDB+サーバーの構成にすべき）。

## 実装上ハマりやすい点（このプロジェクトで実際に踏んだ罠）

1. **`hidden`属性が効かない**：要素側に`display: flex`等を指定していると、UA標準の`[hidden]{display:none}`と詳細度が同点になり、後勝ちで無効化されることがある。`.foo[hidden]{display:none}`を明示的に書く。
2. **flexの子要素がはみ出す**：`flex:1`な子要素（特に`<input>`や画像）は、`min-width:0`を明示しないと内容物のサイズより縮まず、親からはみ出す。
3. **`min-height`と`height`の競合**：親クラスで`min-height:100vh`を指定していると、個別に`height:100dvh`を上書きしても打ち消されない。上書きする側で`min-height:0`も明示する。
4. **透過PNG化にflood-fillだけを使うと、漢字やアイコンの内部にある閉じた白領域が塗り残る**（境界から届かないため）。ロゴ等を透過加工する際は「白色からの距離」ベースでアルファ値を作る方式の方が安全。
5. **ライト/ダーク両対応のロゴ・アイコン画像**は、同じアルファマスクに対して色だけ差し替えた2枚（`xxx.png`/`xxx-dark.png`）を用意し、CSSの`prefers-color-scheme`＋`data-theme`属性で出し分ける。
6. **`navigator.share()`のBlur/Change依存の自動保存**は、アプリがバックグラウンドに回るとイベントが発火せず入力が消える。`visibilitychange`/`pagehide`でも保存する保険が要る。
7. **期間ベースの「今月」判定**（16日締め等）は、今日browsing中の期間（`currentPeriodStart`のようなグローバル変数）と、関数呼び出し時点の「本当の今日」を混同しやすい。自動処理（バックグラウンド送信等）は必ず`new Date()`から独立に期間を計算し直す。

## 新しい業務アイデアへの転用チェックリスト

以下に3つ以上YESが付くなら、この構成がそのまま使える可能性が高い：

- [ ] 記録する人は基本的に1人1台で完結する
- [ ] 提出・共有のタイミングは「都度」ではなく「まとめて」でよい
- [ ] 提出先は既存のメール／チャット／クラウドストレージ（Box等）で受けられる
- [ ] 写真や数値の入力が中心で、複雑な承認フロー等はない
- [ ] オフラインで入力できた方が現場では都合がいい

逆に、リアルタイム性・複数人での同時編集・承認フローが必要になった時点で、サーバー＋DBの構成を検討すべきタイミング。

## 検討中の派生アイデア：工事写真の一時保管＋送信アプリ

「現場で撮った報告用写真をアプリ内に保管しておき、後でまとめてメール作成できる」というアイデア（蔵衛門のような台帳作成機能は不要、シンプルな一時保管＋送信でよい）は、このアプリの写真まわりの実装（撮影→自動リサイズ→IndexedDB保存→期間ごとにまとめて共有シートで送信）をほぼそのまま転用できる。

新規に作り込みが必要になるのはおおよそ以下の部分：
- 今日撮った写真を並べて見る一覧画面（サムネイル一覧、複数選択）
- 送信時に複数枚の写真＋簡単なコメントをまとめて共有シートに渡す処理（`navigator.share({files:[...多数], text:"..."})`は複数ファイル対応済み）
- 台帳的な体裁（キャプション付きの並び）が将来的に欲しくなった場合は、PC側スクリプトでExcel/PDFに整形する層を別途足す（このアプリのtranscribe.py相当）

現時点で「看板・台紙は不要」という前提なら、走行距離アプリの写真部分を8割方流用でき、追加で作るのは一覧画面と複数枚共有の部分だけで済むはず。
